# Environment Variables

## Config

 - `LISTEN_ADDRESS` (default: `:3000`) - Address to listen on
 - `AK_HOST` (**required**) - Authentik domain
 - `AK_APP-NAME` (default: `tailscale`) - Authentik app name
 - `REAL_IP_HEADER` (default: `true`) - Get client IP from the Real-IP header

